diff --git a/src/ets/latex/ekdosis_from_tei.py b/src/ets/latex/ekdosis_from_tei.py
index 6fae201..00df6b5 100644
--- a/src/ets/latex/ekdosis_from_tei.py
+++ b/src/ets/latex/ekdosis_from_tei.py
@@ -1,5 +1,6 @@
 from __future__ import annotations
 
+from dataclasses import dataclass
 from os import PathLike
 from pathlib import Path
 import re
@@ -16,11 +17,28 @@ _TRAILING_SPACE = "\uE000ETS_TRAILING_SPACE\uE000"
 XML_ID = "{http://www.w3.org/XML/1998/namespace}id"
 
 
+@dataclass
+class _RenderContext:
+    apparatus_policy: str = "full"
+    apparatus_numbering_policy: str = "ekdosis"
+    current_line_number: str | None = None
+    line_number_printed: bool = False
+
+    def for_line(self, line_number: str | None) -> "_RenderContext":
+        return _RenderContext(
+            apparatus_policy=self.apparatus_policy,
+            apparatus_numbering_policy=self.apparatus_numbering_policy,
+            current_line_number=_base_line_number(line_number),
+            line_number_printed=False,
+        )
+
+
 def tei_to_ekdosis(
     xml_input: str | PathLike[str],
     *,
     standalone: bool = False,
     apparatus_policy: str = "full",
+    apparatus_numbering_policy: str = "ekdosis",
 ) -> str:
     """Convert canonical ETS TEI XML to a minimal LaTeX-Ekdosis fragment.
 
@@ -28,11 +46,18 @@ def tei_to_ekdosis(
     with ``<`` is treated as XML; otherwise an existing path is read from disk.
     """
     root = _parse_xml_input(xml_input)
+    if apparatus_numbering_policy not in {"ekdosis", "editorial"}:
+        raise ValueError("apparatus_numbering_policy must be 'ekdosis' or 'editorial'.")
+
+    context = _RenderContext(
+        apparatus_policy=apparatus_policy,
+        apparatus_numbering_policy=apparatus_numbering_policy,
+    )
     body = root.find(".//tei:text/tei:body", namespaces=NS)
     if body is None:
         fragment = ""
     else:
-        fragment = "\n".join(line for child in body for line in _render_block(child, apparatus_policy=apparatus_policy))
+        fragment = "\n".join(line for child in body for line in _render_block(child, context=context))
     fragment = _finalize_spacing(fragment).rstrip() + ("\n" if fragment else "")
     if standalone:
         return wrap_standalone(fragment, witness_declarations=render_ekdosis_witness_declarations_from_root(root))
@@ -117,28 +142,28 @@ def _local_name(element: etree._Element) -> str:
     return etree.QName(element).localname
 
 
-def _render_block(element: etree._Element, *, apparatus_policy: str = "full") -> list[str]:
+def _render_block(element: etree._Element, *, context: _RenderContext) -> list[str]:
     tag = _local_name(element)
     if tag == "div":
-        return _render_div(element, apparatus_policy=apparatus_policy)
+        return _render_div(element, context=context)
     if tag == "sp":
-        return _render_speech(element, apparatus_policy=apparatus_policy)
+        return _render_speech(element, context=context)
     if tag == "stage":
-        return [_render_stage(element, apparatus_policy=apparatus_policy)]
+        return [_render_stage(element, context=context)]
     if tag == "head":
-        return [f"\\stage{{{_render_inline(element).strip()}}}"]
+        return [f"\\stage{{{_render_inline(element, context=context).strip()}}}"]
     if tag == "l":
-        return [_render_line(element, apparatus_policy=apparatus_policy)]
+        return [_render_line(element, context=context)]
     if tag == "lg":
-        return _render_lg(element, apparatus_policy=apparatus_policy)
+        return _render_lg(element, context=context)
     if tag == "note":
         return []
     if tag == "front":
         return []
-    return [line for child in element for line in _render_block(child, apparatus_policy=apparatus_policy)]
+    return [line for child in element for line in _render_block(child, context=context)]
 
 
-def _render_div(element: etree._Element, *, apparatus_policy: str = "full") -> list[str]:
+def _render_div(element: etree._Element, *, context: _RenderContext) -> list[str]:
     div_type = element.get("type", "")
     lines: list[str] = []
     if div_type in {"act", "scene"}:
@@ -147,13 +172,13 @@ def _render_div(element: etree._Element, *, apparatus_policy: str = "full") -> l
         lines.append(f"\\ekddiv{{type={div_type}, n={number}, depth={depth}}}")
 
     for child in element:
-        lines.extend(_render_block(child, apparatus_policy=apparatus_policy))
+        lines.extend(_render_block(child, context=context))
     return lines
 
 
-def _render_speech(element: etree._Element, *, apparatus_policy: str = "full") -> list[str]:
+def _render_speech(element: etree._Element, *, context: _RenderContext) -> list[str]:
     speaker = element.find("./tei:speaker", namespaces=NS)
-    speaker_text = _render_inline(speaker, apparatus_policy=apparatus_policy).strip() if speaker is not None else ""
+    speaker_text = _render_inline(speaker, context=context).strip() if speaker is not None else ""
     lines = [
         r"\begin{speech}",
         f"  \\speaker{{{speaker_text}}}",
@@ -163,21 +188,21 @@ def _render_speech(element: etree._Element, *, apparatus_policy: str = "full") -
     for child in element:
         if _local_name(child) == "speaker":
             continue
-        for rendered in _render_block(child, apparatus_policy=apparatus_policy):
+        for rendered in _render_block(child, context=context):
             lines.append(f"    {rendered}")
 
     lines.extend([r"  \end{ekdverse}", r"\end{speech}"])
     return lines
 
 
-def _render_stage(element: etree._Element, *, apparatus_policy: str = "full") -> str:
-    rendered = _render_inline(element, apparatus_policy=apparatus_policy).strip()
+def _render_stage(element: etree._Element, *, context: _RenderContext) -> str:
+    rendered = _render_inline(element, context=context).strip()
     if element.get("type") == "personnages":
         return f"\\stage{{{rendered}}}"
     return f"\\didas{{{rendered}}}"
 
 
-def _render_lg(element: etree._Element, *, apparatus_policy: str = "full") -> list[str]:
+def _render_lg(element: etree._Element, *, context: _RenderContext) -> list[str]:
     lines: list[str] = []
     if element.get("type") == "stanza":
         attrs = ["type=stanza"]
@@ -189,42 +214,43 @@ def _render_lg(element: etree._Element, *, apparatus_policy: str = "full") -> li
 
     for child in element:
         if _local_name(child) == "l":
-            lines.append(_render_line(child, apparatus_policy=apparatus_policy))
+            lines.append(_render_line(child, context=context))
         else:
-            lines.extend(_render_block(child, apparatus_policy=apparatus_policy))
+            lines.extend(_render_block(child, context=context))
     return lines
 
 
-def _render_line(element: etree._Element, *, apparatus_policy: str = "full") -> str:
+def _render_line(element: etree._Element, *, context: _RenderContext) -> str:
     number = element.get("n", "")
-    content = _render_inline(element, apparatus_policy=apparatus_policy).strip()
+    line_context = context.for_line(number)
+    content = _render_inline(element, context=line_context).strip()
     return f"\\vnum{{{number}}}{{{content}\\\\}}"
 
 
-def _render_inline(element: etree._Element | None, *, apparatus_policy: str = "full") -> str:
+def _render_inline(element: etree._Element | None, *, context: _RenderContext) -> str:
     if element is None:
         return ""
 
     output: list[str] = [_render_text_chunk(element.text)]
     for child in element:
-        output.append(_render_inline_child(child, apparatus_policy=apparatus_policy))
+        output.append(_render_inline_child(child, context=context))
         output.append(_render_text_chunk(child.tail))
     return "".join(output)
 
 
-def _render_inline_child(element: etree._Element, *, apparatus_policy: str = "full") -> str:
+def _render_inline_child(element: etree._Element, *, context: _RenderContext) -> str:
     tag = _local_name(element)
     if tag == "app":
-        return _render_app(element, apparatus_policy=apparatus_policy)
+        return _render_app(element, context=context)
     if tag == "hi" and element.get("rend") == "italic":
-        return _render_italic(element, apparatus_policy=apparatus_policy)
+        return _render_italic(element, context=context)
     if tag == "note":
         return ""
-    return _render_inline(element, apparatus_policy=apparatus_policy)
+    return _render_inline(element, context=context)
 
 
-def _render_italic(element: etree._Element, *, apparatus_policy: str = "full") -> str:
-    content = _render_inline(element, apparatus_policy=apparatus_policy)
+def _render_italic(element: etree._Element, *, context: _RenderContext) -> str:
+    content = _render_inline(element, context=context)
     trailing = ""
     while content.endswith(_TRAILING_SPACE):
         content = content[: -len(_TRAILING_SPACE)]
@@ -241,10 +267,10 @@ def _should_hide_app(element: etree._Element, apparatus_policy: str) -> bool:
     return element.get("cert") != "low"
 
 
-def _render_app(element: etree._Element, *, apparatus_policy: str = "full") -> str:
-    if _should_hide_app(element, apparatus_policy):
+def _render_app(element: etree._Element, *, context: _RenderContext) -> str:
+    if _should_hide_app(element, context.apparatus_policy):
         lemma = element.find("./tei:lem", namespaces=NS)
-        return _render_inline(lemma, apparatus_policy=apparatus_policy) if lemma is not None else ""
+        return _render_inline(lemma, context=context) if lemma is not None else ""
 
     pieces: list[str] = [r"\app{"]
     has_trailing_space = False
@@ -252,22 +278,47 @@ def _render_app(element: etree._Element, *, apparatus_policy: str = "full") -> s
         tag = _local_name(child)
         if tag not in {"lem", "rdg"}:
             continue
-        content, trailing_space = _render_reading(child, apparatus_policy=apparatus_policy)
+        content, trailing_space = _render_reading(child, context=context)
         has_trailing_space = has_trailing_space or trailing_space
         wit = _format_wit(child.get("wit", ""))
-        pieces.append(f"\\{tag}[wit={{{wit}}}]{{{content}}}")
+        options = _reading_options(tag, wit, content, context)
+        pieces.append(f"\\{tag}[{options}]{{{content}}}")
     pieces.append("}")
     if has_trailing_space:
         pieces.append(_TRAILING_SPACE)
     return "".join(pieces)
 
 
-def _render_reading(element: etree._Element, *, apparatus_policy: str = "full") -> tuple[str, bool]:
-    rendered = _render_inline(element, apparatus_policy=apparatus_policy)
+def _render_reading(element: etree._Element, *, context: _RenderContext) -> tuple[str, bool]:
+    rendered = _render_inline(element, context=context)
     trailing_space = bool(rendered) and rendered[-1].isspace()
     return rendered.strip(), trailing_space
 
 
+def _reading_options(tag: str, wit: str, content: str, context: _RenderContext) -> str:
+    options: list[str] = [f"wit={{{wit}}}"]
+    if tag == "lem" and context.apparatus_numbering_policy == "editorial":
+        options.append("nonum")
+        prefix = _next_editorial_apparatus_prefix(context)
+        if prefix:
+            options.append(f"alt={{{prefix}{content}}}")
+    return ",".join(options)
+
+
+def _next_editorial_apparatus_prefix(context: _RenderContext) -> str:
+    if not context.current_line_number or context.line_number_printed:
+        return ""
+    context.line_number_printed = True
+    return rf"\textbf{{{context.current_line_number}}}~"
+
+
+def _base_line_number(value: str | None) -> str | None:
+    if value is None:
+        return None
+    base = value.strip().split(".", 1)[0].strip()
+    return base or None
+
+
 def _format_wit(value: str) -> str:
     sigla = [item.lstrip("#") for item in value.split() if item.strip()]
     return ", ".join(sigla)
diff --git a/src/ets/publication_pdf/templates.py b/src/ets/publication_pdf/templates.py
index 01c7952..3027ec6 100644
--- a/src/ets/publication_pdf/templates.py
+++ b/src/ets/publication_pdf/templates.py
@@ -356,7 +356,11 @@ def _has_dramatis_content(fragment: str) -> bool:
 
 
 def _dramatic_fragment(dramatic_xml_path: Path) -> str:
-    fragment = tei_to_ekdosis(dramatic_xml_path, standalone=False).rstrip()
+    fragment = tei_to_ekdosis(
+        dramatic_xml_path,
+        standalone=False,
+        apparatus_numbering_policy="editorial",
+    ).rstrip()
     if not fragment:
         return "\n".join(
             [
diff --git a/tests/test_ekdosis_from_tei.py b/tests/test_ekdosis_from_tei.py
index b0423d5..aef7499 100644
--- a/tests/test_ekdosis_from_tei.py
+++ b/tests/test_ekdosis_from_tei.py
@@ -206,3 +206,41 @@ def test_witness_declarations_reject_apparatus_without_listwit() -> None:
 
     with pytest.raises(ValueError, match="listWit"):
         render_ekdosis_witness_declarations(xml)
+
+
+def test_editorial_apparatus_numbering_injects_tei_line_numbers() -> None:
+    xml = _tei_with_lines(
+        [
+            (
+                "3",
+                'Qu’errant dans le Palais sans suite <app><lem wit="#A">&amp; sans escorte</lem><rdg wit="#B">&amp; escorte</rdg></app>',
+            ),
+            (
+                "27",
+                'Rome depuis <app><lem wit="#A">trois</lem><rdg wit="#B">deux</rdg></app> ans par ses soins gouvernée',
+            ),
+        ]
+    )
+
+    actual = tei_to_ekdosis(xml, apparatus_numbering_policy="editorial")
+
+    assert r"\lem[wit={A},nonum,alt={\textbf{3}~\& sans escorte}]{\& sans escorte}" in actual
+    assert r"\lem[wit={A},nonum,alt={\textbf{27}~trois}]{trois}" in actual
+    assert r"\rdg[wit={B}]{\& escorte}" in actual
+    assert r"\rdg[wit={B}]{deux}" in actual
+
+
+def test_editorial_apparatus_numbering_suppresses_repeated_number_on_same_line() -> None:
+    xml = _tei_with_lines(
+        [
+            (
+                "12",
+                'Premier <app><lem wit="#A">mot</lem><rdg wit="#B">terme</rdg></app> et second <app><lem wit="#A">mot</lem><rdg wit="#B">terme</rdg></app>.',
+            )
+        ]
+    )
+
+    actual = tei_to_ekdosis(xml, apparatus_numbering_policy="editorial")
+
+    assert actual.count(r"alt={\textbf{12}~mot}") == 1
+    assert actual.count(r"\lem[wit={A},nonum") == 2
